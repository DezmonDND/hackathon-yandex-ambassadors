// export const sortArray = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.sortItem > b.sortItem ? 1 : b.sortItem > a.sortItem ? -1 : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.sortItem < b.sortItem ? 1 : b.sortItem < a.sortItem ? -1 : 0
//       );
//   }
// };

// export const sortByName = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userName > b.userName ? 1 : b.userName > a.userName ? -1 : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userName < b.userName ? 1 : b.userName < a.userName ? -1 : 0
//       );
//   }
// };

// export const sortByDate = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userDate > b.userDate ? 1 : b.userDate > a.userDate ? -1 : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userDate < b.userDate ? 1 : b.userDate < a.userDate ? -1 : 0
//       );
//   }
// };

// export const sortBySendMerch = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userSendMerch > b.userSendMerch
//           ? 1
//           : b.userSendMerch > a.userSendMerch
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userSendMerch < b.userSendMerch
//           ? 1
//           : b.userSendMerch < a.userSendMerch
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByTelegram = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userTelegram > b.userTelegram
//           ? 1
//           : b.userTelegram > a.userTelegram
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userTelegram < b.userTelegram
//           ? 1
//           : b.userTelegram < a.userTelegram
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByFeedback = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userFeedback > b.userFeedback
//           ? 1
//           : b.userFeedback > a.userFeedback
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userFeedback < b.userFeedback
//           ? 1
//           : b.userFeedback < a.userFeedback
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByHabr = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userHabr > b.userHabr ? 1 : b.userHabr > a.userHabr ? -1 : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userHabr < b.userHabr ? 1 : b.userHabr < a.userHabr ? -1 : 0
//       );
//   }
// };

// export const sortByCurator = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userCurator > b.userCurator
//           ? 1
//           : b.userCurator > a.userCurator
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userCurator < b.userCurator
//           ? 1
//           : b.userCurator < a.userCurator
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByMerch = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userMerch > b.userMerch
//           ? 1
//           : b.userMerch > a.userMerch
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userMerch < b.userMerch
//           ? 1
//           : b.userMerch < a.userMerch
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByHudiSize = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userHudiSize > b.userHudiSize
//           ? 1
//           : b.userHudiSize > a.userHudiSize
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userHudiSize < b.userHudiSize
//           ? 1
//           : b.userHudiSize < a.userHudiSize
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortBySocksSize = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userSocksSize > b.userSocksSize
//           ? 1
//           : b.userSocksSize > a.userSocksSize
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userSocksSize < b.userSocksSize
//           ? 1
//           : b.userSocksSize < a.userSocksSize
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByIndex = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userIndex > b.userIndex
//           ? 1
//           : b.userIndex > a.userIndex
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userIndex < b.userIndex
//           ? 1
//           : b.userIndex < a.userIndex
//           ? -1
//           : 0
//       );
//   }
// };

// export const sortByHudi = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userHudi > b.userHudi
//           ? 1
//           : b.userHudi > a.userHudi
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userHudi < b.userHudi
//           ? 1
//           : b.userHudi < a.userHudi
//           ? -1
//           : 0
//       );
//   }
// };
// export const sortByCoffee = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userCoffee > b.userCoffee
//           ? 1
//           : b.userCoffee > a.userCoffee
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userCoffee < b.userCoffee
//           ? 1
//           : b.userCoffee < a.userCoffee
//           ? -1
//           : 0
//       );
//   }
// };
// export const sortBySticker = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userSticker > b.userSticker
//           ? 1
//           : b.userSticker > a.userSticker
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userSticker < b.userSticker
//           ? 1
//           : b.userSticker < a.userSticker
//           ? -1
//           : 0
//       );
//   }
// };
// export const sortByPlus = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userPlus > b.userPlus
//           ? 1
//           : b.userPlus > a.userPlus
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userPlus < b.userPlus
//           ? 1
//           : b.userPlus < a.userPlus
//           ? -1
//           : 0
//       );
//   }
// };
// export const sortByArzamas = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userArzamas > b.userArzamas
//           ? 1
//           : b.userArzamas > a.userArzamas
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userArzamas < b.userArzamas
//           ? 1
//           : b.userArzamas < a.userArzamas
//           ? -1
//           : 0
//       );
//   }
// };
// export const sortByShopper = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userShopper > b.userShopper
//           ? 1
//           : b.userShopper > a.userShopper
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userShopper < b.userShopper
//           ? 1
//           : b.userShopper < a.userShopper
//           ? -1
//           : 0
//       );
//   }
// };
// export const sortByBackpack = (arr, orderBy) => {
//   switch (orderBy) {
//     case "asc":
//     default:
//       return arr.sort((a, b) =>
//         a.userBackpack > b.userBackpack
//           ? 1
//           : b.userBackpack > a.userBackpack
//           ? -1
//           : 0
//       );
//     case "desc":
//       return arr.sort((a, b) =>
//         a.userBackpack < b.userBackpack
//           ? 1
//           : b.userBackpack < a.userBackpack
//           ? -1
//           : 0
//       );
//   }
// };